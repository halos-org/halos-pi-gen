# Raspberry Pi images ship multiple kernel variants (v8, 2712) and the
# bootloader selects the correct one for the hardware at boot time.
# needrestart cannot handle this — it picks one variant as "expected"
# and flags a false mismatch against the running kernel.
$nrconf{kernelhints} = 0;
